Nightly builds for PhoneGap.

This repository contains the nightly builds for PhoneGap.  There is no guarantee that the libraries will work correctly.

The builds are located in a directory with the following format:

    Platform-version-date-time

For Android, this directory and its contents are:

    Android-1.2.0-20111101-160000
        - phonegap-1.2.0.jar
        - phonegap-1.2.0.js

NOTE: Only Android is supported right now.

